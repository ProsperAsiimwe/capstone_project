/*
const express = require('express');
const { EmisController } = require('@controllers/Api');

const emisRouter = express.Router();
const controller = new EmisController();

emisRouter.get('/', controller.institutionDetailsFunction);

emisRouter.get('/school-campuses', controller.institutionCampus);

emisRouter.get('/study-level', controller.institutionStudyLevel);

emisRouter.get('/admission-schemes', controller.apiAdmissionSchemes);

emisRouter.get('/sponsors', controller.apiSponsors);

emisRouter.get('/test-client', controller.institutionDetailsFunction);

module.exports = emisRouter;

*/
