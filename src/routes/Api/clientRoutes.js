/*
const express = require('express');
const { ClientController } = require('@controllers/Api');

const clientRouter = express.Router();
const controller = new ClientController();

clientRouter.get('/', controller.clientFunction);

clientRouter.post('/', controller.createClientFunction);

clientRouter.get('/test-client', controller.clientFunction);

clientRouter.post('/test-client', controller.createClientFunction);

module.exports = clientRouter;

*/
